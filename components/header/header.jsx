import React from 'react'
import ThemeButton from '../theme/themeButton'

const Header = () => {
  return (
    <header>
        <nav>
            <ThemeButton/>
        </nav>
    </header>
  )
}

export default Header